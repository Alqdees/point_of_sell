import 'package:flutter/material.dart';
    
class AccountOrders extends StatelessWidget {

  const AccountOrders({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}